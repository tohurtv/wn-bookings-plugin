tabs:
    fields:

        status_enabled:
            label: tohur.bookings::lang.export.status_filter
            type: checkbox
            default: false
            tab: tohur.bookings::lang.export.status_filter_tab
        status_id:
            label: tohur.bookings::lang.export.status_filter_label
            span: full
            type: dropdown
            required: true
            tab: tohur.bookings::lang.export.status_filter_tab
            trigger:
                action: show
                field: status_enabled
                condition: checked
